generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Url {
  id          BigInt   @id @default(autoincrement())
  slug        String   @unique
  original_url String
  created_at  DateTime @default(now())
}
